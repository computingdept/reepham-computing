const ks3HubMatch=(document.body.dataset.hub||'').match(/year-([789])-hub\.html/);if(ks3HubMatch){document.body.dataset.hub='../../index.html#year/'+ks3HubMatch[1]+'/computing/lessons';document.body.dataset.course='Computing lessons'};
const lessonHub=document.body.dataset.hub,lessonCourse=document.body.dataset.course||'Course hub',lessonHome=document.body.dataset.home||'../../index.html';if(lessonHub){const side=document.querySelector('.teacher-nav .brand');if(side){const a=document.createElement('a');a.className='lesson-back-link';a.href=lessonHub;a.setAttribute('aria-label','Back to '+lessonCourse);a.textContent='← '+lessonCourse;side.insertAdjacentElement('afterend',a)}const bar=document.createElement('div');bar.className='mobile-course-bar';bar.setAttribute('aria-label','Lesson navigation');bar.innerHTML='<a href="'+lessonHub+'" aria-label="Back to '+lessonCourse+'">← '+(ks3HubMatch?'Computing lessons':'Hub')+'</a><span class="mobile-course-title">'+lessonCourse+'</span><a href="'+lessonHome+'" aria-label="Computing home">Home</a>';document.body.insertBefore(bar,document.body.firstChild)};const order=['do-now','objectives','new-learning','i-do','we-do','you-do','vocabulary','exit-ticket'];const pages=[...document.querySelectorAll('.page')],nav=[...document.querySelectorAll('.nav-buttons button')],reveal=document.getElementById('reveal'),catchup=document.getElementById('catchup');const date=document.getElementById('lessonDate'),now=new Date();date.textContent=new Intl.DateTimeFormat('en-GB',{weekday:'short',day:'numeric',month:'short',year:'numeric'}).format(now);const modelSteps=[...document.querySelectorAll('.model-step')];let modelIndex=0;function resetModel(){modelIndex=0;modelSteps.forEach((s,i)=>s.classList.toggle('step-visible',i===0))}function showPage(id,hash=true){const p=document.getElementById(id)||document.getElementById('do-now');pages.forEach(x=>x.classList.toggle('active',x===p));nav.forEach(b=>b.classList.toggle('active',b.dataset.page===p.id));document.getElementById('phaseSignal').textContent=p.dataset.phase||'';document.getElementById('behaviourSignal').textContent=p.dataset.behaviour||'';const r=p.dataset.response||'';document.getElementById('responseSignal').hidden=!r;document.getElementById('responseSignal').textContent=r;document.body.classList.remove('show-answers');reveal.setAttribute('aria-pressed','false');reveal.textContent=p.id==='vocabulary'?'Reveal all':'Reveal answers';if(p.id==='i-do')resetModel();if(hash)history.replaceState(null,'','#'+p.id);window.scrollTo({top:0,behavior:'smooth'})}nav.forEach(b=>b.addEventListener('click',()=>showPage(b.dataset.page)));const vocab=[...document.querySelectorAll('.vocab-card')];reveal.addEventListener('click',()=>{const on=document.body.classList.toggle('show-answers');reveal.setAttribute('aria-pressed',String(on));reveal.textContent=on?'Hide answers':'Reveal answers'});catchup.addEventListener('click',()=>{const on=document.body.classList.toggle('catchup-mode');catchup.setAttribute('aria-pressed',String(on));catchup.textContent=on?'Catch-up mode ✓':'Catch-up mode'});resetModel();document.querySelectorAll('.scenario').forEach(card=>{const correct=card.dataset.correct;card.querySelectorAll('.choice').forEach(btn=>btn.addEventListener('click',()=>{card.querySelectorAll('.choice').forEach(b=>b.classList.remove('feedback-good','feedback-bad'));btn.classList.add(btn.dataset.choice===correct?'feedback-good':'feedback-bad');card.querySelector('.choice-feedback').textContent=btn.dataset.choice===correct?'Good choice. Explain why.':'Try again.'}))});vocab.forEach(c=>c.addEventListener('click',()=>c.classList.toggle('revealed')));document.querySelectorAll('.exit-choice').forEach(c=>c.addEventListener('click',()=>document.querySelectorAll('.exit-choice').forEach(b=>b.setAttribute('aria-pressed',String(b===c)))));function advance(){const a=document.querySelector('.page.active');if(a.id==='i-do'&&modelIndex<modelSteps.length-1){modelIndex++;modelSteps[modelIndex].classList.add('step-visible');return}const i=order.indexOf(a.id);if(i<order.length-1)showPage(order[i+1])}window.addEventListener('click',e=>{if(e.target.closest('button,input,a,.vocab-card,.teacher-nav'))return;if(e.clientX>=window.innerWidth*.58)advance()});showPage(location.hash.slice(1)||'do-now',false);